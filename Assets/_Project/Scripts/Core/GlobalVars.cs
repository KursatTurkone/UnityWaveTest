using UnityEngine;

public static class GlobalVars
{
    // Game loop-ish
    public static bool GameIsOver = false;
    public static bool GameIsPaused = false;// I didn't want to change these because they used at player side 
}
